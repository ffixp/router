protocol static {
    ipv6 {
        import all;
        export none;
        preference 110;
    };
}

protocol kernel {
	scan time 60;
    ipv6 {
        import all;
        export none;
    };
}

protocol device {
	scan time 60;
}

protocol direct {
	interface "mesh";
    ipv6 {
        import all;
        export none;
    };
}
